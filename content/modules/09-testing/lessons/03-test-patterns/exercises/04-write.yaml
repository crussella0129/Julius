type: write
id: "03-test-patterns-write-01"
prompt: "Write a function called `safe_divide` that returns the result of a/b, or returns None if b is zero. Then write tests using Arrange-Act-Assert pattern for: normal division, division by zero returning None, and negative number division. Print 'All tests passed!' at the end."
starter_code: |
  # Define safe_divide function

  # Write AAA-pattern tests

  print("All tests passed!")
tests:
  - description: "Output contains 'All tests passed!'"
    check: "'All tests passed!' in output"
  - description: "No errors in output"
    check: "'Error' not in output"
hints:
  - "def safe_divide(a, b): return a / b if b != 0 else None"
  - "Arrange: set up inputs. Act: call function. Assert: check result."
  - "assert safe_divide(10, 2) == 5.0; assert safe_divide(10, 0) is None"
concept_tags:
  - arrange-act-assert
  - edge-cases
